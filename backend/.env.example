# Copy file này thành `.env` (hoặc set trực tiếp env var trước khi chạy Spring Boot).
# Spring Boot tự đọc biến môi trường — không cần dotenv library.

# Database
DB_URL=jdbc:postgresql://localhost:5432/mypham
DB_USERNAME=mypham
DB_PASSWORD=mypham123

# Server
SERVER_PORT=8080
SPRING_PROFILES_ACTIVE=dev

# JWT — PROD phải sinh secret mới (tối thiểu 256-bit, base64-encoded)
# Tạo nhanh: openssl rand -base64 48
JWT_SECRET=dGhpc19pc19hX3ZlcnlfbG9uZ19zZWNyZXRfa2V5X2Zvcl9kZXZlbG9wbWVudF9vbmx5Xzg0MjM=
